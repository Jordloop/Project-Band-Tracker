@{
  Layout ="Shared/_Layout.cshtml";
}
<ul>
  @if (@Model.Count != 0 )
  {
    @foreach (var band in Model)
    {
      <li>
        <a href="/band/@band.GetId()">@band.GetName()</a>
      </li>
    }
  }
  <a href="band/add">Add Band</a>
</ul>
