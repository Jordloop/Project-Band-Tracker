@{
  Layout ="Shared/_Layout.cshtml";
}
<ul>
  @if (@Model.Count != 0 )
  {
    @foreach (var venue in Model)
    {
      <li>@venue.GetName()</li>
    }
  }
  <a href="venue/add">Add Venue</a>
</ul>
